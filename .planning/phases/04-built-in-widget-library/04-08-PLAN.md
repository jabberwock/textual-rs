---
phase: 04-built-in-widget-library
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/textual-rs/src/widget/input.rs
  - crates/textual-rs/tests/widget_tests.rs
autonomous: true
gap_closure: true
requirements:
  - WIDGET-03

must_haves:
  truths:
    - "Input widget accepts an optional validator callback"
    - "Invalid input renders with red-colored text as visual feedback"
    - "Validation runs on each keystroke and on programmatic value changes"
    - "Input::Changed message includes a valid: bool field"
  artifacts:
    - path: "crates/textual-rs/src/widget/input.rs"
      provides: "Input validation API"
      contains: "validator"
    - path: "crates/textual-rs/tests/widget_tests.rs"
      provides: "Validation test coverage"
      contains: "input_validation"
  key_links:
    - from: "crates/textual-rs/src/widget/input.rs"
      to: "Input::Changed message"
      via: "emit_changed includes validation result"
      pattern: "valid.*validator"
---

<objective>
Close Gap 1 from VERIFICATION.md: Add validation support to the Input widget.

Purpose: WIDGET-03 specifies "validation" as a feature of Input. The current implementation has placeholder, password mode, cursor navigation, Changed/Submitted messages, but no validation API or error state rendering. This plan adds a validator callback, validation state tracking, and visual error feedback.

Output: Input widget with validator support, error state rendering, and test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-built-in-widget-library/04-CONTEXT.md
@crates/textual-rs/src/widget/input.rs
@crates/textual-rs/tests/widget_tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validator field, validation logic, and error state rendering to Input</name>
  <files>crates/textual-rs/src/widget/input.rs, crates/textual-rs/tests/widget_tests.rs</files>
  <read_first>
    - crates/textual-rs/src/widget/input.rs (current Input implementation — 383 lines)
    - crates/textual-rs/tests/widget_tests.rs (existing Input tests start at line ~305)
    - crates/textual-rs/src/widget/mod.rs (Widget trait for reference)
    - crates/textual-rs/src/reactive/mod.rs (Reactive<T> API)
  </read_first>
  <behavior>
    - Test 1 (input_validation_valid_input): Create Input with validator `|s| s.len() <= 5`. Type "abc" (3 chars). Verify the rendered text does NOT have red styling (check that the Input's `is_valid()` method returns true).
    - Test 2 (input_validation_invalid_input): Create Input with validator `|s| s.len() <= 3`. Type "abcde" (5 chars). Verify `is_valid()` returns false.
    - Test 3 (input_validation_no_validator_always_valid): Create Input with no validator. Type anything. Verify `is_valid()` returns true (default behavior unchanged).
    - Test 4 (input_validation_changed_message_has_valid_field): Create Input with validator. Type text that fails validation. Verify the Changed message in the message queue has `valid: false`.
  </behavior>
  <action>
    Modify `crates/textual-rs/src/widget/input.rs`:

    1. Add a `valid` field to the `messages::Changed` struct:
       ```rust
       pub struct Changed {
           pub value: String,
           pub valid: bool,
       }
       ```

    2. Add fields to the `Input` struct:
       ```rust
       pub struct Input {
           pub value: Reactive<String>,
           pub placeholder: String,
           pub password: bool,
           validator: Option<Box<dyn Fn(&str) -> bool>>,
           valid: Cell<bool>,
           cursor_pos: Cell<usize>,
           own_id: Cell<Option<WidgetId>>,
       }
       ```

    3. Update `Input::new()` to initialize `validator: None` and `valid: Cell::new(true)`.

    4. Add builder method:
       ```rust
       pub fn with_validator(mut self, f: impl Fn(&str) -> bool + 'static) -> Self {
           self.validator = Some(Box::new(f));
           self
       }
       ```

    5. Add public query method:
       ```rust
       pub fn is_valid(&self) -> bool {
           self.valid.get()
       }
       ```

    6. Add private validation runner:
       ```rust
       fn run_validation(&self) {
           let is_valid = match &self.validator {
               Some(f) => f(&self.value.get_untracked()),
               None => true,
           };
           self.valid.set(is_valid);
       }
       ```

    7. Update `emit_changed` to call `self.run_validation()` before emitting, and include `valid: self.valid.get()` in the Changed message.

    8. Update `render()`: When `self.valid.get()` is false and the input has content, render the text using `Style::default().fg(ratatui::style::Color::Red)` instead of `Style::default()`. The cursor (REVERSED) style should also include the red foreground when invalid: `Style::default().fg(Color::Red).add_modifier(Modifier::REVERSED)`. Placeholder text rendering (dim style) is unaffected by validation state.

    Add tests to `crates/textual-rs/tests/widget_tests.rs` after the existing Input tests (after `input_password_mode`):

    - `input_validation_valid_input`: Create `Input::new("").with_validator(|s| s.len() <= 5)`, type "abc", get the widget from arena via `ctx.arena.get(id)`, downcast to `&Input`, assert `is_valid()` is true.
    - `input_validation_invalid_input`: Create `Input::new("").with_validator(|s| s.len() <= 3)`, type "abcde", assert `is_valid()` is false.
    - `input_no_validator_always_valid`: Create `Input::new("")` (no validator), type "hello", assert `is_valid()` is true.
    - `input_changed_message_includes_valid`: Create `Input::new("").with_validator(|s| !s.is_empty())`, type "a", check Changed message in queue has `valid: true`. Then clear and type to make empty (backspace), check `valid: false`.

    Note: Accessing the Input widget from tests may require downcasting from `&dyn Widget` to `&Input`. If the arena returns `Box<dyn Widget>`, use `(**widget).downcast_ref::<Input>()` or an equivalent pattern. If downcast is not feasible due to trait object limitations, test via the Changed message's `valid` field instead, which is observable through the message queue.
  </action>
  <verify>
    <automated>cd C:/Users/mbeha/code/textual-rs && cargo test input_validation -- --nocapture 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - crates/textual-rs/src/widget/input.rs contains `validator: Option<Box<dyn Fn(&str) -> bool>>`
    - crates/textual-rs/src/widget/input.rs contains `valid: Cell<bool>`
    - crates/textual-rs/src/widget/input.rs contains `pub fn with_validator`
    - crates/textual-rs/src/widget/input.rs contains `pub fn is_valid`
    - crates/textual-rs/src/widget/input.rs contains `fn run_validation`
    - crates/textual-rs/src/widget/input.rs contains `Color::Red` (error state rendering)
    - messages::Changed struct contains `pub valid: bool`
    - crates/textual-rs/tests/widget_tests.rs contains `fn input_validation`
    - `cargo test input_validation` exits 0 with all validation tests passing
    - `cargo test` exits 0 with no regressions (all 215+ tests pass)
  </acceptance_criteria>
  <done>Input widget has validator callback field, runs validation on each change, renders invalid text in red, emits valid flag in Changed message, and has 3+ passing test cases.</done>
</task>

</tasks>

<verification>
- `cargo test input_validation` — all new validation tests pass
- `cargo test` — full suite passes with no regressions (215+ tests)
- `grep -c "validator" crates/textual-rs/src/widget/input.rs` returns 3+
- `grep "valid: bool" crates/textual-rs/src/widget/input.rs` finds the Changed field
</verification>

<success_criteria>
Input widget supports optional validation via `with_validator()`, tracks validity state, renders red text on invalid input, includes `valid: bool` in Changed messages, and all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-built-in-widget-library/04-08-SUMMARY.md`
</output>
