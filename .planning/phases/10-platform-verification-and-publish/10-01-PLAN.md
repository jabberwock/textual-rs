---
phase: 10-platform-verification-and-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PLATFORM-01
must_haves:
  truths:
    - "CI workflow uses the correct dtolnay/rust-toolchain@stable action on all jobs"
    - "CI runs cargo test --workspace on ubuntu-latest, windows-latest, and macos-latest"
    - "CI includes a docs job that fails on missing rustdoc warnings"
    - "CI includes a lint job with clippy and rustfmt checks"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Three-platform CI matrix with test, docs, and lint jobs"
      contains: "dtolnay/rust-toolchain@stable"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions"
      via: "push/PR trigger on master branch"
      pattern: "dtolnay/rust-toolchain@stable"
---

<objective>
Fix the broken GitHub Actions CI so the three-platform test matrix actually runs.

Purpose: PLATFORM-01 requires CI-verified builds on macOS and Linux. The current CI references a nonexistent action (`dtolnay/rust-action/setup@v1`) and has no docs job.
Output: Working `.github/workflows/ci.yml` with test (3 platforms), docs, and lint jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-platform-verification-and-publish/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CI action references and add docs job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/10-platform-verification-and-publish/10-RESEARCH.md
  </read_first>
  <action>
Rewrite `.github/workflows/ci.yml` with three jobs: test, docs, lint.

**test job** (runs on matrix: ubuntu-latest, windows-latest, macos-latest):
1. Replace `dtolnay/rust-action/setup@v1` with `dtolnay/rust-toolchain@stable` (line 24 currently)
2. Keep `actions/checkout@v4` and `actions/cache@v4` steps as-is
3. Keep `cargo build --workspace` and `cargo test --workspace` steps
4. Keep `fail-fast: false` in strategy

**docs job** (runs on ubuntu-latest only):
1. Add new job `docs` with name `Docs`
2. Steps: checkout, rust-toolchain@stable, `cargo doc --no-deps --workspace`
3. Set env var `RUSTDOCFLAGS: "-D warnings"` at job level
4. No cache needed (docs build is fast and one-off)

**lint job** (runs on ubuntu-latest):
1. Replace `dtolnay/rust-action/setup@v1` with `dtolnay/rust-toolchain@stable` (line 50 currently)
2. Keep `with: components: clippy, rustfmt` on the toolchain step
3. Keep `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets -- -D warnings`
4. Keep cache step as-is

The `on:` trigger stays the same (push/PR to main, master). The `env: CARGO_TERM_COLOR: always` stays at top level.
  </action>
  <verify>
    <automated>grep -c "dtolnay/rust-toolchain@stable" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "dtolnay/rust-toolchain@stable" .github/workflows/ci.yml` returns 3 matches (test, docs, lint jobs)
    - `grep "dtolnay/rust-action" .github/workflows/ci.yml` returns 0 matches (old action fully removed)
    - `grep "RUSTDOCFLAGS" .github/workflows/ci.yml` returns 1 match containing `-D warnings`
    - `grep "cargo doc --no-deps --workspace" .github/workflows/ci.yml` returns 1 match
    - `grep "ubuntu-latest, windows-latest, macos-latest" .github/workflows/ci.yml` returns 1 match
    - File contains exactly 3 jobs: test, docs, lint
  </acceptance_criteria>
  <done>CI YAML has correct action references on all 3 jobs, includes a docs job with RUSTDOCFLAGS, and matrix covers all 3 platforms</done>
</task>

<task type="auto">
  <name>Task 2: Push CI fix and verify GitHub Actions runs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
  </read_first>
  <action>
After Task 1 completes, validate the YAML locally:

1. Run `cargo test --workspace` locally to confirm tests still pass on Windows
2. Run `cargo doc --no-deps --workspace` locally to confirm docs build (warnings are expected at this stage, that is OK — the docs job will enforce no-warnings AFTER plan 02 adds docs)

Note: The docs CI job uses `-D warnings` which will initially fail until Plan 02 adds documentation. That is expected — PLATFORM-01 only requires the test matrix to pass, not the docs job. The docs job is added now so Plan 02 does not need to touch ci.yml again.

Verify the YAML is syntactically valid by checking indentation — each job should be at the same indentation level under `jobs:`, each step under `steps:`.
  </action>
  <verify>
    <automated>cd "C:/Users/mbeha/code/textual-rs" && cargo test --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` output ends with "test result: ok" (0 failures)
    - `cargo doc --no-deps --workspace 2>&1 | head -1` does NOT contain "error" (may contain warnings)
    - `.github/workflows/ci.yml` is valid YAML (no tab characters: `grep -P "\t" .github/workflows/ci.yml` returns 0 matches)
  </acceptance_criteria>
  <done>CI YAML validated locally — tests pass, docs build, no YAML syntax errors. Ready for GitHub to run the matrix.</done>
</task>

</tasks>

<verification>
- `grep -c "dtolnay/rust-toolchain@stable" .github/workflows/ci.yml` returns 3
- `grep -c "dtolnay/rust-action" .github/workflows/ci.yml` returns 0
- `cargo test --workspace` passes locally
- CI YAML contains jobs: test (3-platform matrix), docs (with RUSTDOCFLAGS), lint (clippy+fmt)
</verification>

<success_criteria>
The `.github/workflows/ci.yml` uses `dtolnay/rust-toolchain@stable` in all jobs, includes a three-platform test matrix, a docs job with `-D warnings`, and a lint job with clippy+rustfmt. Local `cargo test --workspace` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-platform-verification-and-publish/10-01-SUMMARY.md`
</output>
