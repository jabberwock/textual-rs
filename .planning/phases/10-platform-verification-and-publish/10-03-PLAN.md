---
phase: 10-platform-verification-and-publish
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - crates/textual-rs/Cargo.toml
  - crates/textual-rs-macros/Cargo.toml
  - crates/textual-rs/src/lib.rs
  - CHANGELOG.md
autonomous: false
requirements:
  - PUBLISH-01
  - PUBLISH-03
user_setup:
  - service: crates.io
    why: "Publishing crates requires an API token"
    env_vars:
      - name: CARGO_REGISTRY_TOKEN
        source: "https://crates.io/settings/tokens -> New Token -> name it 'textual-rs-publish' -> scope: publish-new + publish-update"
    dashboard_config:
      - task: "Add CRATES_IO_TOKEN repository secret"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"
must_haves:
  truths:
    - "Both crates have version 0.3.0 in their Cargo.toml"
    - "textual-rs-macros has all required crates.io metadata (description, license, repository)"
    - "textual-rs depends on textual-rs-macros with both path and version fields"
    - "cargo package --list succeeds for both crates with no broken-path warnings"
    - "cargo publish --dry-run succeeds for both crates"
    - "CHANGELOG.md has a [0.3.0] release entry"
    - "lib.rs quick-start snippet shows version 0.3"
  artifacts:
    - path: "crates/textual-rs/Cargo.toml"
      provides: "Main crate with version 0.3.0 and path+version dep on macros"
      contains: 'version = "0.3.0"'
    - path: "crates/textual-rs-macros/Cargo.toml"
      provides: "Macros crate with version 0.3.0 and full crates.io metadata"
      contains: 'version = "0.3.0"'
    - path: "CHANGELOG.md"
      provides: "Release notes for 0.3.0"
      contains: "[0.3.0]"
  key_links:
    - from: "crates/textual-rs/Cargo.toml"
      to: "crates/textual-rs-macros/Cargo.toml"
      via: "path + version dependency"
      pattern: 'textual-rs-macros.*version.*0\.3\.0'
    - from: "crates/textual-rs/src/lib.rs"
      to: "Cargo.toml version"
      via: "quick-start snippet"
      pattern: 'textual-rs = "0\.3"'
---

<objective>
Fix Cargo manifests, align versions to 0.3.0, update CHANGELOG, and verify publish readiness.

Purpose: PUBLISH-01 requires the crate to be published to crates.io with correct metadata. PUBLISH-03 requires `cargo package --list` to produce a clean package. Both crate manifests need fixes before publishing is possible.
Output: Both crates pass `cargo publish --dry-run` and `cargo package --list` cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-platform-verification-and-publish/10-RESEARCH.md
@.planning/phases/10-platform-verification-and-publish/10-01-SUMMARY.md
@.planning/phases/10-platform-verification-and-publish/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Cargo manifests and bump versions to 0.3.0</name>
  <files>
    crates/textual-rs/Cargo.toml
    crates/textual-rs-macros/Cargo.toml
    crates/textual-rs/src/lib.rs
    CHANGELOG.md
  </files>
  <read_first>
    - crates/textual-rs/Cargo.toml
    - crates/textual-rs-macros/Cargo.toml
    - crates/textual-rs/src/lib.rs
    - CHANGELOG.md
  </read_first>
  <action>
**1. Update `crates/textual-rs-macros/Cargo.toml`:**

Change `version = "0.1.0"` to `version = "0.3.0"`.

Add the following fields to the `[package]` section (after `edition.workspace = true`):
```toml
description = "Procedural macros for the textual-rs TUI framework"
license = "MIT"
repository = "https://github.com/mbeha/textual-rs"
keywords = ["tui", "terminal", "macro", "derive"]
categories = ["command-line-interface"]
```

The final `[package]` section should be:
```toml
[package]
name = "textual-rs-macros"
version = "0.3.0"
edition.workspace = true
description = "Procedural macros for the textual-rs TUI framework"
license = "MIT"
repository = "https://github.com/mbeha/textual-rs"
keywords = ["tui", "terminal", "macro", "derive"]
categories = ["command-line-interface"]
```

**2. Update `crates/textual-rs/Cargo.toml`:**

Change `version = "0.1.0"` to `version = "0.3.0"`.

Change the macros dependency from:
```toml
textual-rs-macros = { path = "../textual-rs-macros" }
```
to:
```toml
textual-rs-macros = { path = "../textual-rs-macros", version = "0.3.0" }
```

All other fields stay the same.

**3. Update `crates/textual-rs/src/lib.rs` quick-start snippet:**

Find the line containing `textual-rs = "0.2"` (around line 22) and change it to `textual-rs = "0.3"`.

**4. Update `CHANGELOG.md`:**

Change `## [Unreleased]` to `## [0.3.0] - 2026-03-28 (v1.3 milestone)`.

Add a new `## [Unreleased]` section above it (empty, for future changes):
```markdown
## [Unreleased]

## [0.3.0] - 2026-03-28 (v1.3 milestone)
```

Under the existing `### Added` items in the (now 0.3.0) section, add:
```markdown
- **Widget parity** -- MaskedInput, DirectoryTree, Toast, RichLog, LoadingIndicator, OptionList, SelectionList, ContentSwitcher, Static, Rule, Link, Pretty, Digits
- **Screen stack** -- push/pop/modal screen navigation with focus save/restore
- **Rustdoc coverage** -- all public API items documented; `#![deny(missing_docs)]` enforced
- **Cross-platform CI** -- test matrix on Linux, macOS, Windows with docs and lint jobs
```

**5. Run `cargo check --workspace` to confirm the manifest changes compile.**
  </action>
  <verify>
    <automated>cd "C:/Users/mbeha/code/textual-rs" && cargo check --workspace 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'version = "0.3.0"' crates/textual-rs/Cargo.toml` matches exactly 1 line
    - `grep 'version = "0.3.0"' crates/textual-rs-macros/Cargo.toml` matches exactly 1 line
    - `grep 'textual-rs-macros.*version.*0.3.0' crates/textual-rs/Cargo.toml` matches 1 line
    - `grep 'description' crates/textual-rs-macros/Cargo.toml` matches 1 line
    - `grep 'license = "MIT"' crates/textual-rs-macros/Cargo.toml` matches 1 line
    - `grep 'repository' crates/textual-rs-macros/Cargo.toml` matches 1 line
    - `grep 'textual-rs = "0.3"' crates/textual-rs/src/lib.rs` matches 1 line
    - `grep '\[0.3.0\]' CHANGELOG.md` matches 1 line
    - `cargo check --workspace` exits 0
  </acceptance_criteria>
  <done>Both crates are at version 0.3.0 with correct metadata. textual-rs depends on textual-rs-macros with both path and version. CHANGELOG has [0.3.0] entry. lib.rs quick-start shows 0.3.</done>
</task>

<task type="auto">
  <name>Task 2: Verify package and publish dry-run</name>
  <files>
    crates/textual-rs/Cargo.toml
    crates/textual-rs-macros/Cargo.toml
  </files>
  <read_first>
    - crates/textual-rs/Cargo.toml
    - crates/textual-rs-macros/Cargo.toml
  </read_first>
  <action>
Run packaging verification commands to confirm both crates are ready to publish:

**Step 1: Verify package contents for macros crate:**
```bash
cargo package --list -p textual-rs-macros
```
Expected output: List of files including `Cargo.toml`, `src/lib.rs`, `src/derive_widget.rs`, `src/widget_impl.rs`. No warnings about broken paths.

**Step 2: Verify package contents for main crate:**
```bash
cargo package --list -p textual-rs
```
Expected output: List of all source files plus `README.md`, `Cargo.toml`. No warnings about broken paths. Confirm README.md is present in the list.

**Step 3: Dry-run publish for macros crate:**
```bash
cargo publish --dry-run -p textual-rs-macros
```
Must exit 0. If it warns about anything, fix it.

**Step 4: Dry-run publish for main crate:**
```bash
cargo publish --dry-run -p textual-rs
```
Must exit 0. Note: This may warn that `textual-rs-macros@0.3.0` is not on crates.io yet — that is expected for dry-run and will resolve when macros is published first.

**Step 5: Run full test suite one final time:**
```bash
cargo test --workspace
```

**Step 6: Run full doc build with warnings-as-errors:**
```bash
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace
```

If any step fails, diagnose and fix the issue.
  </action>
  <verify>
    <automated>cd "C:/Users/mbeha/code/textual-rs" && cargo package --list -p textual-rs-macros 2>&1 | head -10 && echo "---" && cargo package --list -p textual-rs 2>&1 | grep "README" && echo "---" && cargo publish --dry-run -p textual-rs-macros 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo package --list -p textual-rs-macros 2>&1 | grep -c "warning"` returns 0
    - `cargo package --list -p textual-rs 2>&1 | grep "README.md"` matches 1 line
    - `cargo package --list -p textual-rs 2>&1 | grep -c "warning"` returns 0
    - `cargo publish --dry-run -p textual-rs-macros 2>&1 | grep -v "warning.*not yet" | grep -c "error"` returns 0
    - `cargo test --workspace` exits 0
    - `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace` exits 0
  </acceptance_criteria>
  <done>Both crates pass `cargo package --list` cleanly with no broken paths. `cargo publish --dry-run` succeeds for both. Full test suite and doc build pass.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Publish to crates.io</name>
  <files>N/A</files>
  <action>
This is a human-action checkpoint. The user must publish both crates to crates.io manually because it requires an authenticated API token that Claude cannot access.

Steps for the user:
1. Ensure `CARGO_REGISTRY_TOKEN` is set (either as env var or via `cargo login`)
2. Publish macros crate first: `cargo publish -p textual-rs-macros`
3. Wait ~60 seconds for crates.io index propagation
4. Publish main crate: `cargo publish -p textual-rs`
5. Verify at https://crates.io/crates/textual-rs that version 0.3.0 appears
6. In a scratch directory, run `cargo add textual-rs` and confirm it resolves to 0.3.0
  </action>
  <verify>
    <automated>echo "Manual verification required - user confirms publish success"</automated>
  </verify>
  <done>Both crates published to crates.io at version 0.3.0. `cargo add textual-rs` resolves correctly.</done>
</task>

</tasks>

<verification>
- `cargo package --list -p textual-rs-macros` succeeds with no warnings
- `cargo package --list -p textual-rs` succeeds with README.md included and no warnings
- `cargo publish --dry-run -p textual-rs-macros` exits 0
- `cargo publish --dry-run -p textual-rs` exits 0 (ignoring "not yet uploaded" warning for macros)
- `cargo test --workspace` passes
- `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace` exits 0
- Both crates at version 0.3.0
</verification>

<success_criteria>
Both crates pass packaging verification and publish dry-run. CHANGELOG documents v0.3.0. After the human publishes, `cargo add textual-rs` resolves to version 0.3.0.
</success_criteria>

<output>
After completion, create `.planning/phases/10-platform-verification-and-publish/10-03-SUMMARY.md`
</output>
